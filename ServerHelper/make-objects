    #!/bin/sh
    DIR=$(cd "$(dirname "$0")"; pwd)
    mogenerator --v2 \
    --model "$DIR/ServerHelperExampleCoreData.xcdatamodeld/ServerHelperExampleCoreData.xcdatamodel" \
    --machine-dir "$DIR/CoreData/Machine" \
    --human-dir "$DIR/CoreData/Human" \
    --swift